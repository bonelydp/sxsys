# 服务器部署后续任务

## 1. 当前部署状态

服务器环境：

- Rocky Linux 9.4
- CPU 架构：x86_64
- 项目目录：`/root/shouyi-deploy`
- 部署方式：Docker Compose

当前已经完成：

- MySQL 8.0 正常运行并通过健康检查
- Spring Boot 后端正常运行
- Vue/Nginx 前端正常运行
- 系统可以通过服务器 IP 的 80 端口访问
- ETE3、PyQt5、NumPy 已安装到后端镜像
- Python 系统发育树绘图环境已经准备完成

当前构建开关：

```env
INSTALL_BIO_TOOLS=false
INSTALL_TREE_RENDERING_DEPS=true
```

当前尚未安装：

- NCBI BLAST+
- MAFFT
- IQ-TREE

因此普通系统功能可以使用，BLAST、MAFFT 和 IQ-TREE 分析功能暂时不可用。

---

## 2. 上传生信软件安装包

需要将以下三个 Linux x86_64 安装包上传到服务器：

```text
bio_software/ncbi-blast-2.15.0+-x64-linux.tar.gz
bio_software/mafft-7.526-gcc_fc6.x86_64.rpm
bio_software/iqtree-3.1.2-Linux-intel.tar.gz
```

Windows PowerShell 上传命令：

```powershell
scp E:\IdeaWorkSpace\bio_software\ncbi-blast-2.15.0+-x64-linux.tar.gz root@服务器IP:/root/shouyi-deploy/bio_software/

scp E:\IdeaWorkSpace\bio_software\mafft-7.526-gcc_fc6.x86_64.rpm root@服务器IP:/root/shouyi-deploy/bio_software/

scp E:\IdeaWorkSpace\bio_software\iqtree-3.1.2-Linux-intel.tar.gz root@服务器IP:/root/shouyi-deploy/bio_software/
```

上传完成后，在服务器检查：

```bash
cd /root/shouyi-deploy
ls -lh bio_software/
```

---

## 3. 安装生信软件

确认三个安装包全部存在后，修改服务器 `.env`：

```bash
cd /root/shouyi-deploy
sed -i 's/^INSTALL_BIO_TOOLS=.*/INSTALL_BIO_TOOLS=true/' .env
grep INSTALL_ .env
```

期望输出：

```text
INSTALL_BIO_TOOLS=true
INSTALL_TREE_RENDERING_DEPS=true
```

重新构建后端镜像：

```bash
docker compose --env-file .env build backend
```

构建过程将安装并验证：

- `blastn`
- `makeblastdb`
- `blastdbcmd`
- `mafft`
- `iqtree3`
- ETE3、PyQt5、NumPy

构建成功后更新后端容器：

```bash
docker compose --env-file .env up -d --no-deps backend
docker compose --env-file .env ps
```

查看后端启动日志：

```bash
docker compose --env-file .env logs --tail=100 backend
```

日志中应出现：

```text
Started ShouyiApplication
```

---

## 4. 验证软件版本

进入后端容器检查：

```bash
docker compose --env-file .env exec backend bash
```

依次执行：

```bash
blastn -version
makeblastdb -version
blastdbcmd -version
mafft --version
iqtree3 -version
python3 -c "from ete3 import Tree; from PyQt5 import QtCore; import numpy; print('Python tree environment OK')"
```

检查完成后退出：

```bash
exit
```

---

## 5. BLAST 参考数据库

参考 FASTA：

```text
bio_data/references/cryptosporidium-18s-reference.fasta
bio_data/references/cryptosporidium-gp60-reference.fasta
```

BLAST 索引目录：

```text
bio_data/blast_db/
```

如果参考 FASTA 没有变化，并且索引文件已经上传，可以直接使用现有索引。

如果更新了参考 FASTA，需要重新生成索引：

```bash
cd /root/shouyi-deploy
docker compose --env-file .env --profile tools run --rm bio-db-init
```

生成后检查：

```bash
ls -lh bio_data/blast_db/
```

---

## 6. 完整生信功能验收

生信软件安装完成并启动后，执行：

```bash
cd /root/shouyi-deploy
docker compose --env-file .env exec -T backend \
  python3 /app/scripts/smoke_test_bio_api.py
```

正常结果应包含：

```text
Sequence lookup: OK
BLAST: OK
MAFFT: OK
IQ-TREE: OK
Tree rendering: OK
all bioinformatics API smoke tests passed
```

该测试覆盖：

1. 参考序列读取
2. BLAST 查询
3. MAFFT 序列比对
4. IQ-TREE 建树
5. Python/ETE3 生成 PNG 树图

---

## 7. 已知的非核心问题

以下问题不影响当前服务器启动和生信软件安装，可以后续处理。

### 7.1 旧数据中的文件地址（已解决）

数据库初始化 SQL 中存在旧地址：

```text
http://localhost:8086/files/...
http://localhost:8087/files/...
```

服务器部署后应逐步改成相对地址：

```text
/api/files/...
```

否则轮播图、通知图片、团队成员图片和部分附件可能无法显示。 

### 7.2 历史上传文件没有迁移（已解决）

旧的上传文件位于本地：

```text
backend/files/
```

Docker 当前使用：

```text
data/files/
```

如果需要恢复旧图片和附件，应将对应文件上传到服务器：

```text
/root/shouyi-deploy/data/files/
```

### 7.3 首页公开数据接口

首页部分统计接口目前要求登录，因此未登录访问时可能显示为空白。后续需要确定：

- 首页统计数据是否允许匿名访问；
- 如果允许，在后端拦截器中将对应查询接口加入公开路径；
- 如果不允许，前端应在未登录状态下隐藏对应区域或显示提示。

### 7.4 前端依赖

前端构建期间会出现部分废弃依赖和安全漏洞提示。当前不影响运行，但后续应：

- 升级 Vue CLI 和相关依赖；
- 执行 `npm audit`；
- 逐项处理高危和严重漏洞；
- 优化较大的前端 JS 包。

---

## 8. 日常维护命令

查看状态：

```bash
cd /root/shouyi-deploy
docker compose --env-file .env ps
```

查看日志：

```bash
docker compose --env-file .env logs --tail=100 backend
docker compose --env-file .env logs --tail=100 frontend
docker compose --env-file .env logs --tail=100 mysql
```

重新启动：

```bash
docker compose --env-file .env restart
```

停止并保留数据库：

```bash
docker compose --env-file .env down
```

代码更新后重新构建：

```bash
docker compose --env-file .env build
docker compose --env-file .env up -d
```

不要在生产服务器执行：

```bash
docker compose down -v
```

该命令会删除 MySQL 数据卷。

---

## 9. 数据备份

备份数据库：

```bash
cd /root/shouyi-deploy
mkdir -p backups

docker compose --env-file .env exec -T mysql \
  sh -c 'mysqldump -u"$MYSQL_USER" -p"$MYSQL_PASSWORD" "$MYSQL_DATABASE"' \
  > "backups/shouyi-$(date +%Y%m%d-%H%M%S).sql"
```

备份上传文件：

```bash
tar -czf "backups/files-$(date +%Y%m%d-%H%M%S).tar.gz" data/files
```

建议在更新系统或数据库结构前执行备份。



